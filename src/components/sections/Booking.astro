---
---

<section id="booking" class="bg-ivory py-16 md:py-24">
  <div class="container-editorial">
    <div class="grid grid-cols-1 md:grid-cols-12 gap-8 md:gap-12 mb-8 md:mb-12">
      <div class="md:col-span-4 reveal">
        <div class="index-label">N° 05 — Booking</div>
      </div>
      <div class="md:col-span-8 reveal">
        <h2 class="h-section max-w-2xl">
          Choose a quiet<br />
          hour, and arrive.
        </h2>
        <p class="mt-5 lede">
          Select a length and pick a time. You will receive a short confirmation
          with the studio address and how to find it.
        </p>
      </div>
    </div>

    <div class="reveal">
      <div class="border border-ink/15 bg-ivory p-2 md:p-3 rounded-[6px] overflow-hidden">
        <div id="cal-booking-inline" style="width:100%;min-width:320px;min-height:720px;"></div>
      </div>
    </div>
  </div>

  <script is:inline>
    Cal("init", "booking", { origin: "https://cal.com" });
    Cal.ns.booking("inline", {
      elementOrSelector: "#cal-booking-inline",
      calLink: "grounded-touch",
      config: { layout: "month_view", theme: "light" },
    });
    Cal.ns.booking("ui", {
      hideEventTypeDetails: false,
      layout: "month_view",
      cssVarsPerTheme: {
        light: {
          "cal-brand": "#A54A28"
        }
      }
    });
  </script>
</section>
