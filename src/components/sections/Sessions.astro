---
const rows = [
  {
    duration: '60',
    price: '65',
    title: 'A short pause',
    text: 'Time enough to settle from head to feet. Ideal for a first visit.',
    calLink: 'grounded-touch/60-min',
    calNs: 'sixty',
    calHref: 'https://cal.com/grounded-touch/60-min?overlayCalendar=true',
  },
  {
    duration: '90',
    price: '85',
    title: 'A longer stay',
    text: 'More room for the body to unwind. The work reaches deeper without pushing.',
    calLink: 'grounded-touch/90-min-thai-massage',
    calNs: 'ninety',
    calHref: 'https://cal.com/grounded-touch/90-min-thai-massage?overlayCalendar=true',
  },
];
---

<section id="sessions" class="bg-ivory py-8 md:py-10">
  <div class="container-editorial">
    <div class="grid grid-cols-1 md:grid-cols-12 gap-8 md:gap-12 mb-10 md:mb-14">
      <div class="md:col-span-4 reveal">
        <div class="index-label">Rates</div>
      </div>
      <div class="md:col-span-8 reveal">
        <h2 class="h-section max-w-2xl">
          Choose your time.
        </h2>
        <p class="mt-5 lede">
          Each session is slow, attentive, and adapted to what your body needs.
        </p>
      </div>
    </div>

    <div class="space-y-4 md:space-y-5">
      {rows.map((r) => (
        <a
          href={r.calHref}
          data-cal-link={r.calLink}
          data-cal-namespace={r.calNs}
          data-cal-config='{"layout":"month_view"}'
          class="reveal grid grid-cols-12 gap-4 md:gap-10 items-start bg-paper hover:bg-line/50 rounded-[6px] px-5 md:px-8 py-7 md:py-10 transition-colors duration-500 ease-gentle"
        >
          <div class="col-span-3 md:col-span-2">
            <span class="font-serif text-5xl md:text-7xl text-ink tabular-nums leading-none">{r.duration}</span>
            <span class="ml-2 index-label">min</span>
          </div>
          <div class="col-span-9 md:col-span-7">
            <h3 class="font-serif text-2xl md:text-3xl text-ink leading-tight">{r.title}</h3>
            <p class="mt-3 text-ink/70 leading-relaxed max-w-prose2">{r.text}</p>
          </div>
          <div class="col-span-12 md:col-span-3 flex items-baseline justify-between md:justify-end gap-6 md:gap-8">
            <div>
              <span class="font-serif text-4xl md:text-5xl text-terra tabular-nums leading-none">{r.price}</span>
              <span class="ml-1 text-2xl md:text-3xl text-stone">€</span>
            </div>
            <span class="btn-link !text-terra">
              Book
              <span aria-hidden="true">→</span>
            </span>
          </div>
        </a>
      ))}
    </div>
  </div>
</section>
